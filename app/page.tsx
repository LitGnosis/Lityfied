import Link from 'next/link'; import { products,money } from '@/lib/catalog'; export default function Home(){return <><p className="eyebrow">The new collection</p><h1>Considered objects for an exceptional everyday.</h1><Link className="button" href="/products">Shop the collection</Link><section className="grid">{products.map(p=><article className="card" key={p.slug}><div className="image"/><h2>{p.name}</h2><p>{p.description}</p><p className="price">{money(p.price)}</p><Link href={`/products/${p.slug}`}>View piece →</Link></article>)}</section></>}
