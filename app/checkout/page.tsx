'use client'; import { useSearchParams } from 'next/navigation'; export default function Checkout(){const product=useSearchParams().get('product'); return <><p className="eyebrow">Checkout</p><h1>Secure payment.</h1><p>{product?`Preparing checkout for ${product}.`:'Choose a piece to begin checkout.'}</p><form action="/api/checkout" method="post"><input type="hidden" name="product" value={product||''}/><button className="button" type="submit">Continue to payment</button></form><p>Payments are processed by Stripe. No card data touches Lityfied servers.</p></>}
