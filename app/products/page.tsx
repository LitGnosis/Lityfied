import { products,money } from '@/lib/catalog'; import Link from 'next/link'; export default function Products(){return <><p className="eyebrow">Collection</p><h1>Permanent pieces, limited quantities.</h1><section className="grid">{products.map(p=><article className="card" key={p.slug}><div className="image"/><h2>{p.name}</h2><p className="price">{money(p.price)}</p><Link href={`/products/${p.slug}`}>View piece →</Link></article>)}</section></>}
