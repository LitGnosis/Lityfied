import { notFound } from 'next/navigation'; import { products,money } from '@/lib/catalog'; import Link from 'next/link'; export default async function Product({params}:{params:Promise<{slug:string}>}){const p=products.find(x=>x.slug===(await params).slug);if(!p)return notFound();return <article><p className="eyebrow">{p.inventory} available</p><div className="grid"><div className="image"/><div><h1>{p.name}</h1><p>{p.description}</p><p className="price">{money(p.price)}</p><Link className="button" href={`/checkout?product=${p.slug}`}>Secure checkout</Link></div></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({'@context':'https://schema.org','@type':'Product',name:p.name,description:p.description,offers:{'@type':'Offer',price:p.price/100,priceCurrency:'USD',availability:p.inventory?'https://schema.org/InStock':'https://schema.org/OutOfStock'}})}}/></article>}
